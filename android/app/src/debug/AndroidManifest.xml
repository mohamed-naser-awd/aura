<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter's debug/profile tooling needs network access (hot reload, observatory). -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
