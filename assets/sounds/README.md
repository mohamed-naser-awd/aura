# Ringtone assets

Place custom ringtone/intense-mode sound files here (e.g. `intense.ogg`). The native
`RingController` currently uses the system default ringtone; switch it to a bundled asset
by reading from this directory.

This file keeps the asset directory present for `pubspec.yaml`.
